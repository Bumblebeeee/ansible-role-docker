---
# defaults file for install_docker
container:
  pre_packages:
    - apt-transport-https
    - ca-certificates
    - curl
    - gnupg-agent
    - software-properties-common
  packages:
    - packages:
      - docker-ce
      - docker-ce-cli
      source:
        repo: "deb [signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu {{ ansible_facts['lsb']['codename'] }} stable"
        keyring:
          source: https://download.docker.com/linux/ubuntu/gpg
          target: /etc/apt/keyrings/docker.gpg
    - containerd.io
    - packages:
      - cri-o
      source:
        repo: "deb [signed-by=/etc/apt/keyrings/cri-o-apt-keyring.gpg] https://pkgs.k8s.io/addons:/cri-o:/stable:/{{ kubernetes_repo_version }}/deb/ /"
        keyring: https://pkgs.k8s.io/addons:/cri-o:/stable:/{{ kubernetes_repo_version }}/deb/Release.key
        keyring_target: /etc/apt/keyrings/cri-o-apt-keyring.gpg
    - packages:
      - conntrack

docker_volume_netshare_type: nfs
docker_volume_netshare_params: ""
